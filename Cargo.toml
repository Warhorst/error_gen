[package]
name = "error_gen"
description = "Transform structs and enums into fully qualified errors using attributes"
version = "1.2.0"
edition = "2018"

[lib]
proc-macro = true

[dependencies]
syn = {version = "1.0.76", features = ["full"]}
quote = "1.0.9"